Hi this is Forrest. I made this website to keep tabs on the tahoe skiing because I'm a ski bum. :)

SETUP:

1. Run `pip3 install -r requirements.txt` to install all necessary modules
2. Run `python3 setup.py`. This downloads and installs geckodriver in src/ to be able to run the scanner.